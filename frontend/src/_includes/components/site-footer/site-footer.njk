{# SETUP #}
{#########}

{# ... #}

{# MARKUP #}
{##########}

<footer class="relative max-w-page-content mx-auto">

    <div class="px-pad-content-section-x pt-pad-content-section-y pb-8
                border-x border-bauturbo-blau">

        <div class="grid grid-cols-12 items-end">

            {# Site footer text (copyright) #}
            <div class="col-span-6 text-sm text-bauturbo-blau">
                {{ siteConfig.footer.text }}
            </div>

            {# Site footer links #}
            {% if siteConfig.footer.menu %}
                <nav class="col-span-6 flex justify-end gap-x-4">
                    {% for item in siteConfig.footer.menu %}
                        <a href="{{ item.url }}" target="_blank" rel="noopener noreferrer"
                           class="text-sm text-bauturbo-blau">
                            {{ item.label }}
                        </a>
                    {% endfor %}
                </nav>
            {% endif %}
        </div>

    </div>

</footer>